
version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8
    environment:
      REACT_APP_TESTMESS: ${REACT_APP_TESTMESS}
      REACT_APP_TESTMES: ${REACT_APP_TESTMES}
    steps:
      - run: 
          name: install
          command: |
            npm install
          
      - run: 
          name: start
          command: |
            npm run build

      - run: 
          name: deploy
          command: |
            firebase deploy